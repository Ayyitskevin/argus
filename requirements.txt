fastapi
uvicorn[standard]
jinja2
python-multipart
pillow
# Optional: HEIC/HEIF decode (pip install pillow-heif)
# pillow-heif

pydantic
httpx
python-dotenv

# Dev / CI
pytest
pytest-asyncio
ruff
# Validates structured-output mode against schemas/vision.schema.json in tests.
jsonschema

# Optional: ARGUS_STORAGE_BACKEND=s3
# boto3
# Optional: ARGUS_REDIS_URL for distributed rate limits
# redis
